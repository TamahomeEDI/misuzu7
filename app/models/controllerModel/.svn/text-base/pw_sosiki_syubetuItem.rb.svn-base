#!/bin/env ruby
# encoding: utf-8
class PwSosikiSyubetuItem
  def initialize(code)
    @組織種別コード = code
  end
  
  def 組織種別コード
      @組織種別コード
    end
  
  def 組織種別コード= (value)
    @組織種別コード = value
  end
  
  def 組織種別名称
    @組織種別名称
  end
   
  def 組織種別名称= (value)
    @組織種別名称 = value
  end
  
  def 更新担当者コード
    @更新担当者コード
  end
   
  def 更新担当者コード= (value)
    @更新担当者コード = value
  end

  def 更新プログラム
    @更新プログラム
  end
   
  def 更新プログラム= (value)
    @更新プログラム = value
  end

  def 更新日時
    @更新日時
  end
   
  def 更新日時= (value)
    @更新日時 = value
  end
  
  def 入力日時
    @入力日時
  end
   
  def 入力日時= (value)
    @入力日時 = value
  end
  
  def 組織階層区分
      @組織階層区分
  end
   
  def 組織階層区分= (value)
    @組織階層区分 = value
  end
  
  def 上位本部組織種別コード
        @上位本部組織種別コード
  end
   
  def 上位本部組織種別コード= (value)
    @上位本部組織種別コード = value
  end

  def puts(hashValue)
    @組織種別コード = hashValue["組織種別コード"];
    @組織種別名称 = hashValue["組織種別名称"];
    @更新担当者コード = hashValue["更新担当者コード"];
    @更新プログラ = hashValue["更新プログラ"];
    @更新日時 = hashValue["更新日時"];
    @入力日時 = hashValue["入力日時"];
    @組織階層区分 = hashValue["組織階層区分"];
    @上位本部組織種別コード = hashValue["上位本部組織種別コード"];
  end
end